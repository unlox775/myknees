# Icon Setup Instructions

## 🎨 **Custom Icon System**

The extension now uses a custom icon build system that allows you to easily update the extension icon.

### 📋 **How to Update the Icon**

1. **Prepare Your Icon**:
   - Create a **128x128 pixel PNG** image
   - Use a **transparent background** (recommended)
   - Save it as `icon.png` in the root directory (`packages/scraped-knees/`)

2. **Build the Icons**:
   ```bash
   make build-icon
   ```

3. **Build the Extension**:
   ```bash
   make build
   ```

4. **Reload the Extension**:
   - Go to `chrome://extensions/`
   - Click the refresh button on your extension
   - The new icon should appear

### 🔧 **What the Build System Does**

- **Copies** your `icon.png` to `src/icons/icon128.png`
- **Webpack** copies the icon to the `dist/icons/` directory
- **Manifest** references the icon for the extension

### 📁 **File Structure**

```
packages/scraped-knees/
├── icon.png              # ← Your custom icon (128x128 PNG)
├── src/icons/
│   └── icon128.png       # ← Generated by build-icon
└── dist/icons/
    └── icon128.png       # ← Final extension icon
```

### 🎯 **Icon Requirements**

- **Size**: 128x128 pixels (minimum)
- **Format**: PNG with transparency
- **Style**: Should represent the extension's purpose (data scraping/AI)
- **Colors**: Should work well with the extension's theme

### 🚀 **Quick Start**

1. Replace `icon.png` with your custom icon
2. Run: `make build-icon && make build`
3. Reload the extension in Chrome

That's it! Your custom icon will now appear in the browser toolbar. 🎉